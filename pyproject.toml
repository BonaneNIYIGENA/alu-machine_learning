[project]
name = "alu-machine-learning"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.4.6",
]

[dependency-groups]
dev = [
    "pycodestyle>=2.14.0",
]
